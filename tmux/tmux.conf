# General
set -g default-terminal 'tmux-256color'
set -ga terminal-overrides ',*:RGB'
set -g mouse on
set -g set-clipboard on
set-window-option -g xterm-keys on

# Colors
thm_bg='#22272E'
thm_fg='#E4E4E4'
thm_gray='#585858'
thm_red='#F47067'
thm_green='#4CAF50'
thm_light_green='#8DDB8C'
thm_blue='#6CB6FF'
thm_light_blue='#96D0FF'
thm_orange='#F69D50'
thm_purple='#DCBDFB'

# Messages
set -g message-style "fg=${thm_light_blue},align=centre"
set -g message-command-style "fg=${thm_light_blue},align=centre"

# Status
set -g status 2
set -g status-format[1] ""
set -g status-style fg=default,bg=default
set -g status-position top
set -g status-interval 1
set -g status-left-length 100
set -g status-right-length 100

# Status (left side)
set -g status-left "#{?client_prefix,#[fg=${thm_red}],#[fg=${thm_blue}]}#[bg=${thm_bg}]#{?client_prefix,#[bg=${thm_red}],#[bg=${thm_blue}]}#[fg=${thm_bg}] #[fg=${thm_fg},bg=${thm_gray}] #S#[fg=${thm_gray},bg=${thm_bg}]"

# Status - Windows (left side)
set -g window-status-separator ''
set -g window-status-activity-style "fg=${thm_fg},bg=${thm_bg},none"
set -g window-status-style "fg=${thm_fg},bg=${thm_bg},none"
set -g window-status-current-format " #[fg=${thm_green}]#[bg=${thm_bg}]#[fg=${thm_bg},bg=${thm_green}]#I #[fg=${thm_fg},bg=${thm_gray}] #(echo '#{pane_current_path}' | rev | cut -d'/' -f-2 | rev)#[fg=${thm_gray},bg=${thm_bg}]"
set -g window-status-format " #[fg=${thm_gray}]#[bg=${thm_bg}]#[fg=${thm_green},bg=${thm_gray}]#I #[fg=${thm_fg},bg=${thm_gray}] #W#[fg=${thm_gray},bg=${thm_bg}]"

# Status (right side)
set -g status-right " #[fg=${thm_light_blue},bg=${thm_bg}]#[fg=${thm_bg},bg=${thm_light_blue}] #[fg=${thm_fg},bg=${thm_gray}] #(kubectl config current-context || echo -)#[fg=${thm_gray},bg=${thm_bg}]"
set -ag status-right " #[fg=${thm_orange},bg=${thm_bg}]#[fg=${thm_bg},bg=${thm_orange}] #[fg=${thm_fg},bg=${thm_gray}] #(whoami || echo -)@#(hostname || echo -)#[fg=${thm_gray},bg=${thm_bg}]"
set -ag status-right " #[fg=${thm_purple},bg=${thm_bg}]#[fg=${thm_bg},bg=${thm_purple}] #[fg=${thm_fg},bg=${thm_gray}] #(date +'%a %b %d %I:%M %p')#[fg=${thm_gray},bg=${thm_bg}]"

# Panes
set -g pane-border-style "fg=${thm_gray}"
set -g pane-active-border-style "fg=${thm_blue}"

# TODO: investigate why these don't work
# Meta + Arrow keys to switch panes
# bind -n M-Left  select-pane -L
# bind -n M-Right select-pane -R
# bind -n M-Up    select-pane -U
# bind -n M-Down  select-pane -D

# Meta + [number] keys to switch window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Splitting
unbind '"'
unbind %
bind -  split-window -v -c "#{pane_current_path}"
bind \\ split-window -h -c "#{pane_current_path}"
bind _ select-layout even-horizontal
bind | select-layout even-horizontal
bind + select-layout tiled

# Start numbering at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Popups
bind C-k display-popup -w 80% -h 80% -E "k9s"

# Reload
unbind r
bind r source-file ~/.config/tmux/tmux.conf
